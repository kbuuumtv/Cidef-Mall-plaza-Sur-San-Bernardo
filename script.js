const menuBtn=document.getElementById("menuBtn");const nav=document.getElementById("nav");menuBtn.addEventListener("click",()=>nav.classList.toggle("open"));document.querySelectorAll("#nav a").forEach(a=>a.addEventListener("click",()=>nav.classList.remove("open")));const colorName=document.getElementById("colorName");const carColor=document.getElementById("carColor");document.querySelectorAll(".swatch").forEach(swatch=>swatch.addEventListener("click",()=>{document.querySelectorAll(".swatch").forEach(s=>s.classList.remove("active"));swatch.classList.add("active");colorName.textContent=swatch.dataset.color;carColor.style.background=swatch.dataset.bg;}));const galleryMain=document.getElementById("galleryMain");document.querySelectorAll(".thumbs button").forEach(button=>button.addEventListener("click",()=>{galleryMain.querySelector("span").textContent=button.dataset.label;const index=[...document.querySelectorAll(".thumbs button")].indexOf(button);const backgrounds=["linear-gradient(135deg,#333,#888)","linear-gradient(135deg,#151515,#666)","linear-gradient(135deg,#777,#222)","linear-gradient(135deg,#222,#aaa)"];galleryMain.style.background=backgrounds[index];}));document.querySelectorAll(".safety-list button").forEach(button=>button.addEventListener("click",()=>{const active=button.classList.toggle("active");button.querySelector("b").textContent=active?"−":"+";}));document.getElementById("quoteForm").addEventListener("submit",event=>{event.preventDefault();const message=document.getElementById("formMessage");message.textContent="Solicitud preparada correctamente. Conecta este formulario a tu servidor o CRM para recibir los datos.";message.classList.add("success");});